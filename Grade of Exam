#include <stdio.h>
int main()
{
  int per;
  printf ( " Enter Percentage :- " );
  scanf ( " %d ",&per );
  if (per>=50)
  {
    printf ( " Pass " );
  }
  else
  {
    printf ( " Fail " );
  }
  
  return 0;
  
}
