#include<stdio.h>
int main()
{
	int a,b,temp;
	printf ( " Enter the value of the a and b ");
	scanf("%d%d",&a,&b);
	temp=a;
	a = b;
	b = temp;
	printf (" The swap value of the a and b is %d,%d",a ,b);
	return 0;
}
